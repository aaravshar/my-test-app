```yaml
services:
  web:
    build: .
    ports:
      - "5000:5000"
    healthcheck:
      test: curl --fail http://localhost:5000/ || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
```
