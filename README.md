# Linear Search Project

## Description

This project includes a linear search implementation and unit tests to verify its accuracy.

## Running the Tests

To ensure everything is working fine, you can run the tests using the following command:

```bash
python -m unittest discover
```

This will automatically discover and run all unittests in files named `test*.py`.

Ensure you have navigated to the project directory before running this command.
